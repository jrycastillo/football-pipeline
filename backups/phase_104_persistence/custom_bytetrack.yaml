# Ultralytics YOLO 🚀, AGPL-3.0 license
# ByteTrack tracker settings for Football Analysis (High Stability)

tracker_type: bytetrack  # tracker type, ['botsort', 'bytetrack']
track_high_thresh: 0.5  # threshold for the first association
track_low_thresh: 0.1   # threshold for the second association
new_track_thresh: 0.6   # threshold for init new track if the detection does not match any tracks
track_buffer: 120       # buffer to calculate the time when to remove tracks (Critical for buffer)
match_thresh: 0.75      # Relaxed from 0.85 to handle sprints
fuse_score: true        # Required for later Ultralytics versions
